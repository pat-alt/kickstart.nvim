#' Add two values together
#'
#' @param a A numeric value.
#' @param b A numeric value.
#' @return The sum of `a` and `b`.
#' @examples
#' myfun(1, 2)
myfun <- function(a, b) {
        return(a + b)
}
